spring:
  application:
    name:service-consumer

server:
  port: 8888

service-provider:
  ribbon:
    eureka:
      enabled: false
    listOfServers: localhost:8099,localhost:8098,localhost:8097
    ServerListRefreshInterval: 15000
